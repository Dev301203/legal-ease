# Production environment variables
# These values should be overridden in your deployment platform (e.g., Render, Vercel, etc.)
# or via .env.production.local (which is git-ignored)

# Set this to your production backend API URL
VITE_API_URL=https://your-api-domain.com

# Mail catcher is typically not used in production
MAILCATCHER_HOST=
